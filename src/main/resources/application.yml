spring:
      jpa:
      show-sql: true
      hibernate:
        ddl-auto: validate
      profiles:
        active: dev
      initialization-mode: always

      thymeleaf:
          mode: HTML
      devtools:
        restart:
          exclude: templates/**,static/**
server:
  error:
    whitelabel:
      enabled: false
